#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void) {
    /*srand((unsigned)time(0));*/
    for (int i = 0; i < 10; i++) {
          long rnd;
  double temp;
  int r;
       int max = 20;
      r = int(max*rand()/(RAND_MAX+1.0)); 
      r -= 10;
  printf("%d\n",r);
        
        }
        int homies;
        scanf("%d",&homies);
        return 0;
        }
